import { Source } from './source';
import { Token } from './token';
import { TokenKind } from './token-kind';
declare class Lexer {
    private source;
    token: Token;
    lastToken: Token;
    private line;
    private lineStart;
    constructor(source: Source);
    advance(): Token;
    lookahead(): Token;
    isIgnoredToken(kind: TokenKind): boolean;
    readNextToken(start: number): Token;
    createToken(kind: TokenKind, start: number, end: number, value?: string | null): Token;
    readName(start: number): Token;
    readDescription(start: number): Token;
    unwrapDescription(description: string): string;
}
export { Lexer };
//# sourceMappingURL=lexer.d.ts.map