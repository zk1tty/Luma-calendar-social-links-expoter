import { Page } from 'playwright';
declare const DOM_NETWORK_QUIET_THRESHOLD_MS: number;
declare const NETWORK_QUIET_THRESHOLD_MS: number;
declare const NETWORK_QUIET_OR_MULTIPLE_REQUESTS_THRESHOLD_MS: number;
declare class PageActivityMonitor {
    private page;
    private requestLog;
    private responseLog;
    private lastNetworkActivityTime;
    private multipleRequestFound;
    private multipleRequestUrl;
    private pageLoaded;
    constructor(page: Page);
    start(): Promise<void>;
    monitorDomChange(): Promise<void>;
    reset(): void;
    private onRequest;
    private onResponse;
    private onLoad;
    getLoadStatus(): boolean;
    isPageReady(startTime: number, lastActiveDomTime?: number): boolean;
}
export { PageActivityMonitor, DOM_NETWORK_QUIET_THRESHOLD_MS, NETWORK_QUIET_THRESHOLD_MS, NETWORK_QUIET_OR_MULTIPLE_REQUESTS_THRESHOLD_MS, };
//# sourceMappingURL=page-monitor.d.ts.map